package database

// TODO implement meee
func UpdateDatabaseEntry[T any]() bool {

	// First select the wanted query -> query has no Question marks -> we add them
	// Query: Update User VALUES -> then we add what we want to be changed!

	return false
}
